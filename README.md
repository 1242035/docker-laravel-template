# docker-laravel-template

1. STRUCT
project
       docker/
       spotech-backend/
       .dockerignore
       docker-compose.yml
       api.dockerfile
       
       
 
 2.run with docker-compose: "docker-compose up --build"
